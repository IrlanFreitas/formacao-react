{
  "name": "admin",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npx json-server --watch db.json",
    "front": "browser-sync start --server --file . --host --port 5000 --startPath telas/lista_cliente.html"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.26.14",
    "json-server": "^0.16.2"
  }
}
